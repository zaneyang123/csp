#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a[] = {0, 10, 20, 30, 40};

    cout << (lower_bound(a, a+5, 15) - a) << endl;

    cout << (1 << 3) << endl;

    cout << (log2(500050)) << endl;

    cout << INT_MIN << endl;
}